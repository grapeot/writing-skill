# No environment variables required for the lint CLI.
# This file exists to satisfy the public-repo scaffold convention.
# If future extensions need config, add fake placeholders here, e.g.:
# EXAMPLE_API_KEY=replace-with-your-real-key